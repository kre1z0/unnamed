import React from "react";
import PropTypes from "prop-types";

const Javascript = () => {
  return <div>ggwp no re Javascript</div>;
};

Javascript.propTypes = {
  component: PropTypes.bool,
};

export default Javascript;
