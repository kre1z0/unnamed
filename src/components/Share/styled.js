import styled from "styled-components";

export const ShareContainer = styled.div`
  display: flex;
  > div {
    display: flex;
    cursor: pointer;
    outline: none;
    margin-right: 10px;
  }
`;
